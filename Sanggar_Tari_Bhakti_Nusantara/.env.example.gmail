# ============================================
# KONFIGURASI GMAIL SMTP UNTUK LARAVEL
# ============================================
# File ini berisi contoh konfigurasi untuk mengirim email melalui Gmail
# Salin nilai-nilai ini ke file .env Anda

# --- Pengaturan Aplikasi ---
APP_NAME="Bhakti Nusantara"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000

# --- Pengaturan Database ---
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sanggar_tari
DB_USERNAME=root
DB_PASSWORD=

# --- KONFIGURASI EMAIL GMAIL (SMTP) ---
# Penting: Gmail memerlukan "App Password" jika Anda mengaktifkan 2FA
# Langkah-langkah:
# 1. Buka https://myaccount.google.com/security
# 2. Aktifkan "2-Step Verification" jika belum
# 3. Kembali ke Security, cari "App passwords"
# 4. Pilih "Mail" dan "Windows Computer" (atau device Anda)
# 5. Google akan menghasilkan password 16 karakter khusus untuk aplikasi
# 6. Gunakan password tersebut di MAIL_PASSWORD (tanpa spasi)

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=xxxx xxxx xxxx xxxx    # 16-character App Password (tanpa spasi saat copy)
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@gmail.com
MAIL_FROM_NAME="Bhakti Nusantara"

# --- Alternatif: MAILTRAP (untuk testing lokal) ---
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=your_mailtrap_username
# MAIL_PASSWORD=your_mailtrap_password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=no-reply@yourdomain.test
# MAIL_FROM_NAME="Bhakti Nusantara"

# --- Pengaturan Session & Cache ---
SESSION_DRIVER=cookie
CACHE_DRIVER=file
QUEUE_CONNECTION=sync

# --- Kunci Aplikasi (generate dengan: php artisan key:generate) ---
APP_KEY=base64:your-generated-key-here
